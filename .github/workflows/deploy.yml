info for `actions/upload-artifact@v3`. It appearsname: Deploy Quartz site to GitHub Pages

on:
  push that there is a typo or incorrect action name in your:
    branches:
      - main
  workflow_dispatch:

 workflow. The correct action name should be `actionspermissions:
  contents: read
  pages: write
  id-token/upload-pages-artifact@v1`, which you are already using correctly for uploading the artifact in the `build: write

concurrency:
  group: "pages"
  cancel-in` job.

Here is the corrected workflow file:

```yaml-progress: false

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v
name: Deploy Quartz site to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

3
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v3
        with:
          node# Sets permissions of the GITHUB_TOKEN to allow deployment-version: 18.x
          cache: npm
      - name: Install Dependencies
        run: npm ci
      - name: Build to GitHub Pages
permissions:
  contents: read
  pages Quartz
        run: npx quartz build
      - name:: write
  id-token: write

# Allow only one concurrent Upload artifact
        uses: actions/upload-art deployment, skipping runs queued between the run inifact@v3
        with:
          name: build
         -progress and latest queued.
concurrency:
  group: path: public

  deploy:
    needs: build
    environment "pages"
  cancel-in-progress: false

jobs:
  build:
      name: github-pages
      url: ${{ steps.de:
    runs-on: ubuntu-22.04
    steps:
      - usesployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages: actions/checkout@v3
        with:
          fetch
        id: deployment
        uses: actions/deploy-depth: 0 # Fetch all history for git info
      --pages@v1